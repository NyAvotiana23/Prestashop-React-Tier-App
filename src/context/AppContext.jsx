import {createContext} from "react";

export const AdminUserContext = createContext(null);

export const CustomerUserContext = createContext(null);

export const CartContext = createContext(null);







